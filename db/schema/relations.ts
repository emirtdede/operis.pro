/**
 * Drizzle ORM Relations definitions.
 * Centralized table-to-table relations registry for relational querying API.
 */

// Currently, Operis uses direct SQL foreign key references (.references(() => table.id)).
// Relational definitions can be added here and exported.
export {};
